class FlightData:
    def __init__(self, price, city_code_from, city_code_to, city_from, city_to, utc_arrival, utc_departure):
        self.price = price
        self.city_code_from = city_code_from,
        self.city_code_to = city_code_to
        self.city_from = city_from
        self.city_to = city_to
        self.utc_arrival = utc_arrival
        self.utc_departure = utc_departure
